import { IPathname } from "../types/User";
// import * as data1 from "../data/data.json";

export function loadFirewireData(
  viewer: Cesium.Viewer,
  pathname: IPathname
): Promise<any> {
  return;
}
